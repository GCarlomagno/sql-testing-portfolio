# Local SQL Test Database

This folder contains the SQLite database environment used to execute the SQL validation scenarios described in `SQL-for-QA.md`.

Files:

- schema.sql – database structure
- seed-data.sql – controlled test dataset
- qa-test-database.db – generated SQLite database